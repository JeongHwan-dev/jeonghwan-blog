name: Generate Label Files

on:
  pull_request:
    paths:
      - '.github/labels/labels.config.mjs'

jobs:
  sync-labels:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate label files
        run: pnpm generate:labels

      - name: Check for changes
        id: verify-changed-files
        run: |
          if [ -n "$(git status --porcelain .github/labels/labels.json .github/labels/labeler.yml)" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            git diff .github/labels/labels.json .github/labels/labeler.yml || true
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Commit changes
        if: steps.verify-changed-files.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .github/labels/labels.json .github/labels/labeler.yml
          git commit -m "chore(label): labels.config.mjs 업데이트에 따라 관련 파일 자동 동기화"
          git push origin HEAD:${{ github.head_ref }}
